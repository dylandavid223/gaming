<footer class="cyber-footer">
  <div class="footer-grid">
    <div class="footer-section">
      <h3 class="footer-title">APOCALYPTIC_DESIGNS</h3>
      <p class="footer-text">
        We annihilate bland corporate aesthetics and reconstruct radical visual
        identities
      </p>
    </div>
    <div class="footer-section">
      <h3 class="footer-title">CONTACT_US</h3>
      <p class="footer-contact">
        <i class="fas fa-envelope"></i> contact@apocalypse.design
      </p>
      <p class="footer-contact"><i class="fas fa-phone"></i> +1 555-DESTROY</p>
    </div>
    <div class="footer-section">
      <h3 class="footer-title">SOCIAL_MEDIA</h3>
      <div class="social-links">
        <a
          href="https://twitter.com/"
          class="social-link"
          target="_blank"
          title="Twitter"
          ><i class="fab fa-twitter"></i
        ></a>
        <a
          href="https://instagram.com/"
          class="social-link"
          target="_blank"
          title="Instagram"
          ><i class="fab fa-instagram"></i
        ></a>
        <a
          href="https://facebook.com/"
          class="social-link"
          target="_blank"
          title="Facebook"
          ><i class="fab fa-facebook"></i
        ></a>
        <a
          href="https://youtube.com/"
          class="social-link"
          target="_blank"
          title="YouTube"
          ><i class="fab fa-youtube"></i
        ></a>
        <a
          href="https://github.com/"
          class="social-link"
          target="_blank"
          title="GitHub"
          ><i class="fab fa-github"></i
        ></a>
      </div>
    </div>
  </div>
  <div class="copyright">
    <p>© 2025 APOCALYPTIC_DESIGNS. ALL SYSTEMS DESTROYED.</p>
  </div>
</footer>

<!-- Font Awesome CDN for social icons (add if not already in your main layout) -->
<link
  rel="stylesheet"
  href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css"
/>

<style>
  /* Cyberpunk Footer Styles */
  .cyber-footer {
    background-color: rgba(5, 1, 14, 0.95);
    border-top: 1px solid var(--neon-purple);
    padding: 3rem 2rem 1rem;
    color: var(--cyber-text);
    position: relative;
    overflow: hidden;
  }

  .cyber-footer::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      135deg,
      transparent 0%,
      rgba(255, 42, 109, 0.05) 50%,
      transparent 100%
    );
    pointer-events: none;
  }

  .footer-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .footer-section {
    padding: 1rem;
    position: relative;
  }

  .footer-section::after {
    content: "";
    position: absolute;
    bottom: -1rem;
    left: 0;
    width: 100%;
    height: 1px;
    background: linear-gradient(
      to right,
      transparent,
      var(--neon-blue),
      transparent
    );
  }

  .footer-title {
    color: var(--neon-green);
    font-size: 1.2rem;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 1.5rem;
    position: relative;
    display: inline-block;
  }

  .footer-title::after {
    content: "";
    position: absolute;
    bottom: -5px;
    left: 0;
    width: 100%;
    height: 2px;
    background: var(--neon-green);
    transform: scaleX(0);
    transform-origin: right;
    transition: transform 0.3s ease;
  }

  .footer-title:hover::after {
    transform: scaleX(1);
    transform-origin: left;
  }

  .footer-text {
    line-height: 1.6;
    margin-bottom: 1rem;
  }

  .footer-contact {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.8rem;
  }

  .footer-contact i {
    color: var(--neon-pink);
    width: 20px;
    text-align: center;
  }

  .social-links {
    display: flex;
    gap: 1rem;
    margin-top: 1rem;
  }

  .social-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border: 1px solid var(--neon-blue);
    color: var(--neon-blue);
    text-decoration: none;
    transition: all 0.3s;
    position: relative;
    overflow: hidden;
    font-size: 1.3rem;
    background: transparent;
  }

  .social-link::before {
    content: "";
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(5, 217, 232, 0.4),
      transparent
    );
    transition: 0.5s;
  }

  .social-link:hover {
    color: var(--dark-bg);
    background-color: var(--neon-blue);
    box-shadow: 0 0 10px var(--neon-blue);
  }

  .social-link:hover::before {
    left: 100%;
  }

  .copyright {
    text-align: center;
    margin-top: 3rem;
    padding-top: 1rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    font-size: 0.8rem;
    color: var(--neon-purple);
  }

  /* Footer Animation */
  @keyframes footer-scan {
    0% {
      transform: translateY(-100%);
    }
    100% {
      transform: translateY(100%);
    }
  }

  /* Responsive Footer */
  @media (max-width: 768px) {
    .footer-grid {
      grid-template-columns: 1fr;
    }

    .footer-section::after {
      bottom: 0;
    }
  }
</style>
