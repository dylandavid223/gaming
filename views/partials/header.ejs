<header class="cyber-header">
  <div class="cyber-logo" data-text="APOCALYPTIC_DESIGNS">
    <span class="logo-text">APOCALYPTIC_DESIGNS</span>
    <span class="logo-glitch"></span>
  </div>
  <nav class="cyber-nav">
    <ul class="nav-list">
      <li><a href="/client-dashboard" class="nav-link">Home</a></li>
      <li><a href="/games" class="nav-link">Games</a></li>
      <li><a href="/developers" class="nav-link">Developers</a></li>
      <li><a href="/library" class="nav-link">Library</a></li>
      <li><a href="/profile" class="nav-link">Profile</a></li>
      <li>
        <a href="/logout" class="nav-link" style="color: #ff2a6d">Logout</a>
      </li>
    </ul>
  </nav>
</header>

<style>
  /* Cyberpunk Header Styles */
  .cyber-header {
    background-color: rgba(5, 1, 14, 0.95);
    border-bottom: 1px solid var(--neon-pink);
    padding: 1rem 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    position: sticky;
    top: 0;
    z-index: 1000;
    box-shadow: 0 0 20px rgba(255, 42, 109, 0.2);
    backdrop-filter: blur(5px);
  }

  .cyber-logo {
    position: relative;
    color: var(--neon-blue);
    font-size: 1.8rem;
    font-weight: bold;
    text-transform: uppercase;
    letter-spacing: 3px;
    text-shadow: 0 0 10px var(--neon-blue);
    padding: 0.5rem 1rem;
    border: 1px solid var(--neon-blue);
    box-shadow: 0 0 15px var(--neon-blue);
  }

  .cyber-logo::before {
    content: "";
    position: absolute;
    top: -5px;
    left: -5px;
    right: -5px;
    bottom: -5px;
    border: 1px solid var(--neon-purple);
    animation: pulse 2s infinite alternate;
  }

  .logo-glitch {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: var(--neon-blue);
    opacity: 0;
    z-index: -1;
    animation: glitch-anim 5s infinite linear;
  }

  .cyber-nav .nav-list {
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
    gap: 1rem;
  }

  .nav-link {
    text-decoration: none;
    position: relative;
    overflow: hidden;
  }

  .nav-link::before {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: var(--neon-pink);
    transform: scaleX(0);
    transform-origin: right;
    transition: transform 0.3s ease;
  }

  .nav-link:hover::before {
    transform: scaleX(1);
    transform-origin: left;
  }

  @keyframes pulse {
    0% {
      opacity: 0.5;
    }
    100% {
      opacity: 1;
    }
  }

  @keyframes glitch-anim {
    0% {
      opacity: 0.1;
      transform: translate(0);
    }
    20% {
      opacity: 0.3;
      transform: translate(-5px, 5px);
    }
    40% {
      opacity: 0.1;
      transform: translate(-5px, -5px);
    }
    60% {
      opacity: 0.3;
      transform: translate(5px, 5px);
    }
    80% {
      opacity: 0.1;
      transform: translate(5px, -5px);
    }
    100% {
      opacity: 0.3;
      transform: translate(0);
    }
  }

  /* Responsive Header */
  @media (max-width: 768px) {
    .cyber-header {
      flex-direction: column;
      padding: 1rem;
    }

    .cyber-logo {
      margin-bottom: 1rem;
      font-size: 1.5rem;
    }

    .cyber-nav .nav-list {
      flex-wrap: wrap;
      justify-content: center;
    }
  }
</style>
